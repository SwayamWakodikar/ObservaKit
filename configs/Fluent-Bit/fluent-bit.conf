[SERVICE]
    Flush 1
    Daemon Off
    Log_level info
    # Parsers_File parsers.conf

[INPUT]
    Name tail
    Path /logs/app.sample.log
    Tag app.logs
    Read_from_Head On

[OUTPUT]
    Name stdout
    Match *
    # Brokers host.docker.internal:9092
    # Format json
    # Topics logs-topic